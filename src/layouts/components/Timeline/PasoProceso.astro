---
const { numero, titulo, descripcion, imagen } = Astro.props;
---

<div class='process-step'>
  <div class='process-step__number'>
    {numero}
  </div>
  <div class='process-step__content'>
    <h3 class='process-step__title'>{titulo}</h3>
    <p class='process-step__description'>{descripcion}</p>
    {imagen && <img src={imagen} alt={titulo} class="process-step__image" />}
  </div>
</div>